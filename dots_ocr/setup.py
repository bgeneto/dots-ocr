from setuptools import setup, find_packages

setup(
    name="dots_ocr",
    version="0.1.0",
    packages=find_packages(),   # will pick up any directory with __init__.py
    python_requires=">=3.8",
)
